let UserName = "raja odedara";
let Age = 20;
let Address = "kinderkheda";


console.log(`hello my name is ${UserName} my age is ${Age}, i live in a village named ${Address}.`);

let userInformation = {
    UserName : "raja odedara",
    Age : 20,
    Address : "kinderkheda"
};

console.table(userInformation);

/* primitive data types
 1. string
 2. number
 3. boolean
 4. null
 5. undefined
 6.symbol*/

/* non-primitive data tpes (refrence)
 1. array
 2. object
 3. function */